import React from 'react';
import { Button } from 'react-native';

const setCharacter = props => {
    return(
        <Button onPress={this.beginCharacterSelection} title={'Set Character'} />
    );
}

beginCharacterSelection = () => {
    return;
}

export default setCharacter;